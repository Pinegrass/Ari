# Ari Continuity Index
Startup: current state, handoff, task, YAML, Git status/log/delta. Product `10-PRODUCT.md`; architecture `30-ARCHITECTURE.md`; decisions `40-DECISIONS.md`; roadmap `50-ROADMAP.md`. Mobile source `src/`; migrations `supabase/`; nested repositories `backend/` and `aritomo-web/` have independent Git histories and must be checked for cross-surface work. The nested `aritomo-web/` checkout is authoritative; the separate `D:\Codex\Workex\aritomo-web` checkout is older. Detailed commands, dependency/API/model inventories, conventions and deployment notes moved from root startup context to `guides/development-reference.md`. Release evidence is under `docs/`, `RELEASE.md`, and `LAUNCH_CHECKLIST.md`.
