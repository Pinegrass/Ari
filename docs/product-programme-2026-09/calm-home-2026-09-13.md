# Calm daily Home — 13 September 2026

Owner approved the discussed simpler daily Home. Mobile-only implementation; no backend/web edits, publish, native build or on-device testing.

## Result

- Compact localized date/greeting with 44×48 privacy and Updates buttons.
- One Spent today hero, smaller monthly spending context and Plan to payday route. Uses complete locale formatting, preserving paise instead of stripping decimal points. Unknown monthly total shows a dash; initial daily loading is explicit.
- At most one existing server-selected Tomo nudge, shown as a compact title/action with dismissal. Private Mode suppresses it and its impression event. This reuses existing selection; it does not introduce a new bill/plan/report arbitration engine.
- Three recent editable entries immediately below; View all opens full Transactions. Empty users retain first-entry CTA. Existing bottom + remains primary capture, now with localized accessible label.
- Compact Money reports link opens the existing detailed reviews. Charts/category analysis, repeated daily totals, stacked shortcuts, engagement/streak and full coaching cards removed from Home. Planning, Updates, Transactions and reports stay reachable; bill/recurring tools remain in Accountant. Underlying components/data are retained.
- Retained monthly summary component now labels the difference as Recorded net, preserves its sign/currency, and removes the misleading saved percentage, including its Private Mode leak. This component is no longer mounted on Home.
- New Home controls/copy translated in English/Hindi. Existing transaction-row category/date copy and native/provider language completeness are outside this change.

## Verification

Focused tests: 3 suites / 11 tests pass; typed catalogs, Home navigation/edit/history, three-row limit, empty capture, compact nudge open/dismiss, privacy/impression suppression, Hindi controls and honest signed monthly net. TypeScript and changed-source ESLint pass.

Actual Dashboard/BalanceCard/NudgeCard/TransactionItem components rendered through React Native Web with synthetic contexts. English 390×844 visually reviewed: daily summary, three rows and review link visible above the illustrative tab bar. Hindi+nudge 320×740 wraps with no horizontal overflow (DOM scroll width equals client width). Empty and private states inspected, private figures masked and nudge absent. Preview error log query returned none; server's missing favicon404 is fixture-only.

Preview limitations: synthetic account/data, mocked API/navigation/privacy hooks, stand-in icons and illustrative bottom bar, web font rendering. A privacy click did not rerender the initial fixture wrapper; private state was verified through its explicit fixture URL and production callback dispatch through component tests. These checks are not native rendering, real API or OS accessibility acceptance. Fixture/server stayed under D:/Codex/Artifacts/Ari/home-preview, outside the repository, and server was stopped.

Full-suite result and exact source are recorded below. Latest published Android update remains net-sign fix01a09a28; this Home redesign is local until an authorized release.

Final source: `c02374a227d45b9cf82c5aed352721965a9e0b1a`. Full mobile Jest: **52 suites / 546 tests passed**. TypeScript, changed-source ESLint and diff whitespace checks pass. No native dependencies/config changed. Source committed locally; not published or pushed.
